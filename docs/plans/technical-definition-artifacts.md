# Plan: Technical Definition Artifacts

> Status: Draft
> Work item: item 1
> Source documents: `docs/research/lightweight-sor-to-github-project-delivery-workflow-artifacts.md` (`Executive Summary`; `Recommended Artifact Set`; `Required Content of the New Artifacts` > `technical-definition` skill, `Technical-definition agent set`, and `Targeted instruction`; `Recommendation`); `docs/research/sor-to-github-project-implementation-planning-workflow.md` (`Recommended End-to-End Process` > `Stage 2: Elicit and Baseline Technical Requirements` and `Stage 3: Select and Record the Technical Approach`; `Minimum Reusable Templates` > `Technical requirement` and `ADR` rows); `.github/skills/statement-of-requirements/SKILL.md`; `.github/skills/prompt-me/SKILL.md`; `.github/agents/requirements-orchestrator.agent.md`; `.github/agents/requirements-author.agent.md`; `.github/agents/requirements-reviewer.agent.md`; `.github/skills/instruction-authoring/SKILL.md`; `docs/requirements/azure-landing-zone.md`.
> Prepared: 2026-08-05
> Human approver: Martyn Fewtrell (repository default)

## 1. Objective

Deliver five repository Copilot customization artifacts that create and independently review a Draft technical-document baseline from an approved SOR, while preserving human technical sign-off and the existing SOR workflow.

## 2. Source Extraction

### Source material for this Draft plan

- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Executive Summary`: proposed technical-definition capability consists of a skill, orchestrator, editable author, read-only reviewer, and scoped instruction; it must reuse the existing SOR orchestration pattern and retain named-human technical sign-off.
- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Recommended Artifact Set`: proposed targets, tool boundaries, delegate allowlists, and prohibition on technical documents outside `docs/technical/` are specified for the five in-scope artifacts.
- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Required Content of the New Artifacts` > `technical-definition` skill: an approved SOR, named destination, technical owner, and sign-off authority are required inputs; the Draft baseline contains linked technical requirements, proportionate design, ADRs when consequential, verification, uncertainty records, review record, and an SOR-change trigger.
- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Technical-definition agent set`: the orchestrator elicits essential unknowns through `prompt-me`, delegates only author then independent reviewer work, returns material changes for correction and re-review, and never signs off or begins planning.
- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Targeted instruction`: the original recommendation includes technical and planning rules. This work item adopts only the technical-document rules: direct Markdown location under `docs/technical/`, SOR ID preservation, proportionate `TR-###` and `ADR-###` identifiers, technical/SOR boundary, peer review/sign-off, and escalation of material scope changes.
- `lightweight-sor-to-github-project-delivery-workflow-artifacts.md` > `Recommendation`: all recommendations are planning inputs only; no implementation approval is supplied for this work item.
- `sor-to-github-project-implementation-planning-workflow.md` > `Stage 2: Elicit and Baseline Technical Requirements`: technical requirements require IDs, parent SOR IDs, category, constraints, rationale, priority, interface/data impacts, verification method, criterion, owner, and status; the exit gate is traceable, internally consistent, testable requirements without unapproved solution choices.
- `sor-to-github-project-implementation-planning-workflow.md` > `Stage 3: Select and Record the Technical Approach`: design must make boundaries, data flows, integrations, operations, and verification reviewable; ADRs record consequential choices and unresolved ADRs remain owned discovery or blockers; a technical authority accepts direction or records owned unresolved choices.
- `sor-to-github-project-implementation-planning-workflow.md` > `Minimum Reusable Templates` > `Technical requirement` and `ADR` rows: minimum field sets define the expected technical-requirement and ADR structures.
- `.github/agents/requirements-orchestrator.agent.md`, `.github/agents/requirements-author.agent.md`, and `.github/agents/requirements-reviewer.agent.md`: existing repository pattern uses constrained orchestrator/author/reviewer roles, YAML tool declarations, Draft-only authoring, read-only review, findings-first output, `prompt-me`, and named-human approval.
- `.github/skills/statement-of-requirements/SKILL.md`: SORs remain solution-free, Draft until human baseline approval, and require a controlled change path; technical content belongs outside the SOR.
- `.github/skills/prompt-me/SKILL.md`: one essential decision is elicited at a time with evidence-grounded numbered options and an explicit free-text option.
- `.github/skills/instruction-authoring/SKILL.md`: path instructions require a narrow `applyTo` glob, concise evidence-based rules, frontmatter/path validation, and a check that the glob neither misses intended nor loads unrelated files.
- `docs/requirements/azure-landing-zone.md` > `Status`, `Stakeholders and Governance`, `External Technical-Design Dependencies`, and `DEC-015`: this is an approved SOR example; its baseline is approved, technical-design approval remains outstanding, and Martyn Fewtrell is the technical owner and approval authority.

### Implementation approval status

- Status: Draft planning input only. The research recommends these artifacts but does not authorize their implementation.
- Decision or approver: Martyn Fewtrell must approve this plan for implementation. Before any generated technical baseline can cease being Draft, its named non-author technical sign-off authority must record approval; the plan does not grant that authority to an agent.

### Constraints

- Scope is limited to the five target files in the target map and to technical-document rules in the instruction file. Source: `Recommended Artifact Set`, `Technical-definition agent set`, and `Targeted instruction`.
- The skill and editable author create or revise Markdown technical documents only directly under `docs/technical/`; neither edits an SOR, approves output, accepts risk, creates delivery Issues, or begins delivery planning. Source: `Recommended Artifact Set` and `technical-definition` skill.
- The orchestrator uses only `read`, `search`, and `agent`, delegates only to the two technical workers, and never edits or signs off. The reviewer uses only `read` and `search`, never edits, approves, or accepts risk. Source: `Recommended Artifact Set` and `Technical-definition agent set`.
- The target instruction must not include planning-document or GitHub-Issue rules. Source: user-requested scope boundary; `Targeted instruction` is a broader proposed input.

### Unresolved or excluded source material

- Delivery-planning skill and agents, GitHub Project/MCP artifacts, Project templates, prompts, hooks, Actions, and plugins: explicitly excluded by this work item; do not create, modify, or validate them.
- Exact `applyTo` behavior for the target VS Code environment: validate discovery with Customization Diagnostics, response References, or Agent Debug Logs if available. If none is available, record that limitation for human review rather than claiming loading was proven. Source: `.github/skills/instruction-authoring/SKILL.md`.
- Exact technical-document template layout: no existing `docs/technical/` template is supplied. Preserve the required fields and allow the skill to select a proportionate document form; stop rather than inventing a repository template or mandatory category taxonomy beyond `TR-###` and `ADR-###` examples.

## 3. Scope Boundary

**Change in this work item**

- Create `.github/skills/technical-definition/SKILL.md` as a user-invocable approved-SOR-to-Draft-technical-baseline workflow.
- Create the three technical agents with the stated user-invocability, tool, delegation, edit, review, and human-approval boundaries.
- Create `.github/instructions/technical-and-planning-documents.instructions.md` with a narrow `applyTo: "docs/technical/**/*.md"` and technical-document rules only.

**Do not change**

- Existing SOR skills, agents, prompts, instructions, or the approved SOR example.
- Delivery-planning, GitHub Project, MCP, project-template, prompt, hook, GitHub Action, plugin, implementation, deployment, or approval records.
- The SOR's business requirements, baseline status, technical-design decisions, or individual requirement acceptance.

## 4. Preconditions

- [ ] The repository is at the requested work-item baseline and contains the referenced SOR workflow artifacts.
- [ ] Martyn Fewtrell has supplied implementation approval for this Draft plan before any target file is edited; otherwise retain this plan as Draft and stop.
- [ ] A supplied baseline reference identifies an SOR with explicit recorded baseline approval before any target file is edited and before the future skill is exercised. `docs/requirements/azure-landing-zone.md` is an approved-SOR example through `DEC-015`, but it is not the missing supplied baseline reference and no technical-design approval is implied.
- [ ] VS Code Customization Diagnostics, response References, or Agent Debug Logs may be available for instruction-discovery validation. Their absence is a documented limitation that permits `Ready for review`; a failed available check or demonstrated scope mismatch is `Blocked`.
- [ ] Read the source anchors, `.github/skills/instruction-authoring/SKILL.md`, and the existing requirements agent manifests before implementation.

## 5. Target Map

| Target | Locate / current state | Required change | Preserve |
| --- | --- | --- | --- |
| `.github/skills/technical-definition/SKILL.md` | New user-invocable skill; mirror frontmatter conventions in `.github/skills/statement-of-requirements/SKILL.md`. | Define approved-SOR input gate, `prompt-me` unknown handling, Draft-only `docs/technical/` authoring, SOR-linked technical requirement fields, proportionate design, ADR criteria/fields, verification, uncertainty/review record, sign-off gate, and SOR-change escalation. | SOR solution-free boundary; no planning, Issue, or approval authority. |
| `.github/agents/technical-orchestrator.agent.md` | New user-facing coordinator; mirror `.github/agents/requirements-orchestrator.agent.md`. | Declare `tools: [read, search, agent]`, `agents: [technical-author, technical-reviewer]`, `user-invocable: true`; define confirm, prompt, author, independent-review, correction/re-review, and Draft-status reporting steps. | No edit, sign-off, risk acceptance, delivery planning, or delegates beyond the named workers. |
| `.github/agents/technical-author.agent.md` | New non-user-invocable editable worker; mirror `.github/agents/requirements-author.agent.md`. | Declare `tools: [read, search, edit]`, `user-invocable: false`; require use of `technical-definition`, direct `docs/technical/*.md` output, required baseline content, readable Markdown validation, Draft status, and uncertainty visibility. | Do not edit SORs, approve own work, accept risk, create delivery items, or use terminal/external/MCP tools. |
| `.github/agents/technical-reviewer.agent.md` | New non-user-invocable review worker; mirror `.github/agents/requirements-reviewer.agent.md`. | Declare `tools: [read, search]`, `user-invocable: false`; provide findings-first checks for technical requirement/ADR fields, SOR traceability, design feasibility, interfaces/data/security/operations/quality where applicable, verification, risks, dependencies, Markdown readability, and sign-off readiness. | Read-only independence; no editing, approval, risk acceptance, or invented facts. |
| `.github/instructions/technical-and-planning-documents.instructions.md` | New targeted instruction; follow `.github/skills/instruction-authoring/SKILL.md`. | Add valid YAML frontmatter with `applyTo: "docs/technical/**/*.md"`; state technical Markdown location, source-ID preservation, proportionate identifiers, solution/plan separation, Draft peer-review/sign-off gate, and escalation to SOR workflow for material business-scope changes. | No planning-document, committed-Issue, MCP, GitHub Project, or unrelated rules. |

## 6. Ordered Tasks

| Step | Action | Expected output | Verify before continuing | On failure or ambiguity | State on successful check |
| --- | --- | --- | --- | --- | --- |
| 1 | Confirm Martyn Fewtrell's implementation approval and the separately supplied approved-SOR baseline reference; inspect only the five target paths and established SOR manifests. | Approval and baseline-reference evidence, plus target-map confirmation, or a precise blocker. | From repository root, run `pwsh -NoProfile -Command "$required=@('.github/skills/statement-of-requirements/SKILL.md','.github/agents/requirements-orchestrator.agent.md','.github/agents/requirements-author.agent.md','.github/agents/requirements-reviewer.agent.md','docs/requirements/azure-landing-zone.md'); $missing=$required | Where-Object { -not (Test-Path $_) }; if($missing){throw ('Missing prerequisite paths: '+($missing -join ', '))}; 'PASS: repository prerequisites'"`; it exits `0` with `PASS: repository prerequisites`. Separately inspect the human approval record and supplied baseline reference; both must identify this work item and an approved SOR. | **Blocked** when either human input is absent, a required source path is missing, or a target conflicts with the established manifest pattern. Do not edit or infer either human decision. | Ready for review |
| 2 | Create `.github/skills/technical-definition/SKILL.md` as the user-invocable approved-SOR-to-Draft-baseline skill. | A discoverable skill whose filename and `name` are both `technical-definition`; its frontmatter declares `user-invocable: true`; its workflow accepts an approved SOR and named destination/owner/sign-off authority, uses `prompt-me` for essential unknowns, writes only Markdown directly under `docs/technical/`, keeps output Draft, requires the stated technical-requirement and ADR fields, requires named non-author sign-off, and stops/escalates instead of planning, approving, or changing an SOR. | From repository root, run `pwsh -NoProfile -Command "$p='.github/skills/technical-definition/SKILL.md'; $raw=Get-Content -Raw $p; $parts=$raw -split '(?m)^---\r?\n',3; $body=$parts[2]; function Need($condition,$message){if(-not $condition){throw $message}}; Need (Test-Path $p) 'missing skill'; Need ($raw -match '(?m)^name: technical-definition$') 'skill name mismatch'; Need ($raw -match '(?m)^user-invocable: true$') 'skill is not user-invocable'; Need ($body -match '(?is)approved SOR.*named.*destination.*technical owner.*sign-off') 'input gate contract missing'; Need ($body -match '(?is)prompt-me.*essential unknown') 'unknown-handling contract missing'; Need ($body -match '(?is)only Markdown.*docs/technical/') 'technical output boundary missing'; Need ($body -match '(?is)Draft.*named non-author.*sign-off') 'Draft approval gate missing'; Need ($body -match '(?is)ID.*parent SOR ID.*statement.*category.*constraints.*rationale.*priority.*interface/data impacts.*verification method.*criterion.*owner.*status') 'technical-requirement field contract missing'; Need ($body -match '(?is)ID.*title.*status.*context.*decision.*options.*consequences.*linked requirements/risks.*owner.*decision date') 'ADR field contract missing'; Need ($body -match '(?is)do not.*planning|do not.*approve|do not.*edit.*SOR') 'prohibited-activity contract missing'; Need ($body -match '(?is)approved SOR.*docs/technical/.*Draft') 'permitted scenario missing'; Need ($body -match '(?is)outside.*docs/technical/|unapproved SOR.*stop') 'invalid-input stop scenario missing'; 'PASS: skill contract'"`; it exits `0` with `PASS: skill contract`. | **Blocked** for any failed frontmatter, input, unknown-handling, output-boundary, Draft, field, sign-off, prohibition, permitted-scenario, or invalid-scenario assertion. Do not substitute planning, approval, or SOR edits. | Ready for review |
| 3 | Create `.github/agents/technical-orchestrator.agent.md` as the user-facing, non-editing coordinator. | An agent with filename/name consistency (`technical-orchestrator`), exact `tools: [read, search, agent]`, exact `agents: [technical-author, technical-reviewer]`, `user-invocable: true`, the six-step confirm/prompt/author/review/correction/re-review/report procedure, and explicit prohibition on edits, sign-off, risk acceptance, and delivery planning. A valid scenario proceeds only for an approved SOR and `docs/technical/` destination; an invalid destination or missing baseline/sign-off authority stops before delegation. | From repository root, run `pwsh -NoProfile -Command "$p='.github/agents/technical-orchestrator.agent.md'; $raw=Get-Content -Raw $p; function Need($condition,$message){if(-not $condition){throw $message}}; Need (Test-Path $p) 'missing orchestrator'; Need ($raw -match '(?m)^name: technical-orchestrator$') 'orchestrator name mismatch'; Need ($raw -match '(?m)^tools: \[read, search, agent\]$') 'orchestrator tool allowlist mismatch'; Need ($raw -match '(?m)^agents: \[technical-author, technical-reviewer\]$') 'orchestrator delegate allowlist mismatch'; Need ($raw -match '(?m)^user-invocable: true$') 'orchestrator invocation mismatch'; Need ($raw -match '(?is)1\. Confirm.*approved SOR.*docs/technical/.*sign-off authority.*2\. Use `prompt-me`.*3\. Delegate.*technical-author.*4\. Delegate.*technical-reviewer.*5\. .*correction.*re-review.*6\. Report') 'orchestrator procedure missing'; Need ($raw -match '(?is)Do not.*edit.*sign off.*accept risk.*delivery planning') 'orchestrator forbidden actions missing'; Need ($raw -match '(?is)approved SOR.*docs/technical/') 'orchestrator valid scenario missing'; Need ($raw -match '(?is)invalid destination|missing.*baseline|missing.*sign-off authority.*stop') 'orchestrator stop scenario missing'; 'PASS: orchestrator contract'"`; it exits `0` with the PASS message. | **Blocked** for a failed frontmatter, allowlist, procedure, prohibition, valid-scenario, or stop-scenario assertion. Do not widen tools or delegates to repair a failure. | Ready for review |
| 4 | Create `.github/agents/technical-author.agent.md` as the non-user-invocable editable technical-document worker. | An agent with filename/name consistency (`technical-author`), exact `tools: [read, search, edit]`, `user-invocable: false`, required use of `technical-definition`, Draft-only direct `docs/technical/` Markdown output, required technical baseline content and Markdown readability procedure, and explicit prohibition on SOR edits, approval, risk acceptance, delivery Issues, terminal, external, and MCP tools. A valid scenario writes a Draft within the allowed directory; an invalid destination stops without editing. | From repository root, run `pwsh -NoProfile -Command "$p='.github/agents/technical-author.agent.md'; $raw=Get-Content -Raw $p; function Need($condition,$message){if(-not $condition){throw $message}}; Need (Test-Path $p) 'missing author'; Need ($raw -match '(?m)^name: technical-author$') 'author name mismatch'; Need ($raw -match '(?m)^tools: \[read, search, edit\]$') 'author tool allowlist mismatch'; Need ($raw -match '(?m)^user-invocable: false$') 'author invocation mismatch'; Need ($raw -match '(?is)Use the `technical-definition` skill') 'author skill invocation missing'; Need ($raw -match '(?is)only Markdown files directly under `docs/technical/`.*Draft') 'author output boundary missing'; Need ($raw -match '(?is)technical requirement.*ADR.*verification.*uncertaint|Verify.*readable Markdown') 'author required procedure missing'; Need ($raw -match '(?is)Do not.*edit.*SOR.*approve.*accept risk.*delivery Issues.*terminal.*external.*MCP') 'author forbidden actions missing'; Need ($raw -match '(?is)docs/technical/.*Draft') 'author valid scenario missing'; Need ($raw -match '(?is)outside.*docs/technical/.*stop.*without editing') 'author stop scenario missing'; 'PASS: author contract'"`; it exits `0` with the PASS message. | **Blocked** for a failed frontmatter, tool, procedure, boundary, prohibition, valid-scenario, or stop-scenario assertion. Do not use a tool outside the exact allowlist. | Ready for review |
| 5 | Create `.github/agents/technical-reviewer.agent.md` as the non-user-invocable, read-only independent reviewer. | An agent with filename/name consistency (`technical-reviewer`), exact `tools: [read, search]`, `user-invocable: false`, a findings-first review procedure covering technical-requirement and ADR fields, traceability, feasibility, applicable interfaces/data/security/operations/quality, verification, risks/dependencies, Markdown readability, and sign-off readiness; it explicitly forbids edits, approval, risk acceptance, and invented facts. A valid Draft review returns findings; a request to edit or approve stops and reports the boundary. | From repository root, run `pwsh -NoProfile -Command "$p='.github/agents/technical-reviewer.agent.md'; $raw=Get-Content -Raw $p; function Need($condition,$message){if(-not $condition){throw $message}}; Need (Test-Path $p) 'missing reviewer'; Need ($raw -match '(?m)^name: technical-reviewer$') 'reviewer name mismatch'; Need ($raw -match '(?m)^tools: \[read, search\]$') 'reviewer tool allowlist mismatch'; Need ($raw -match '(?m)^user-invocable: false$') 'reviewer invocation mismatch'; Need ($raw -match '(?is)findings-first.*technical requirement.*ADR.*traceability.*feasibility.*interfaces.*data.*security.*operations.*quality.*verification.*risks.*dependencies.*Markdown.*sign-off') 'reviewer procedure missing'; Need ($raw -match '(?is)Do not.*edit.*approve.*accept risk.*invent') 'reviewer forbidden actions missing'; Need ($raw -match '(?is)Draft.*findings') 'reviewer valid scenario missing'; Need ($raw -match '(?is)request.*edit|request.*approve.*stop') 'reviewer stop scenario missing'; 'PASS: reviewer contract'"`; it exits `0` with the PASS message. | **Blocked** for a failed frontmatter, tool, procedure, prohibition, valid-scenario, or stop-scenario assertion. Keep the worker read-only. | Ready for review |
| 6 | Create `.github/instructions/technical-and-planning-documents.instructions.md` containing technical-document rules only. | Valid frontmatter with exactly `applyTo: "docs/technical/**/*.md"`; rules for direct technical Markdown location, SOR-ID preservation, proportionate `TR-###`/`ADR-###` IDs, technical/SOR boundary, Draft peer-review/sign-off gate, and material-business-scope escalation; no planning, Issue, MCP, or Project rules. | From repository root, run `pwsh -NoProfile -Command "$p='.github/instructions/technical-and-planning-documents.instructions.md'; $raw=Get-Content -Raw $p; function Need($condition,$message){if(-not $condition){throw $message}}; Need (Test-Path $p) 'missing instruction'; Need ($raw -match '(?m)^applyTo: \"docs/technical/\*\*/\*.md\"$') 'applyTo mismatch'; Need ($raw -match '(?is)docs/technical/.*SOR.*TR-###.*ADR-###.*Draft.*peer review.*sign-off.*material.*scope.*SOR workflow') 'technical instruction contract missing'; Need (-not ($raw -match '(?i)docs/planning|GitHub Issue|\bMCP\b|\bProject\b')) 'excluded planning rule found'; $match='docs/technical/contracts/example.md'; $nonMatch='docs/planning/contracts/example.md'; Need ($match -match '^docs/technical/.+\.md$') 'representative matching technical Markdown path failed'; Need (-not ($nonMatch -match '^docs/technical/.+\.md$')) 'representative nonmatching Markdown path matched'; 'PASS: instruction contract and representative scope'"`; it exits `0` with the PASS message. The path assertions demonstrate the intended scope but do not claim VS Code loading. | **Blocked** for a failed frontmatter/rule/scope contract or demonstrated scope mismatch. Do not add planning rules to make the file broader. | Ready for review |
| 7 | Validate the cross-artifact orchestration contract after the skill and all three agents exist. | The coordinator can invoke only the author and reviewer; the skill is Draft-only with named non-author sign-off; the orchestrator reports Draft status without sign-off or approval; the author is Draft-only and cannot self-approve; and the reviewer is read-only, cannot approve, and reports sign-off readiness. | From repository root, run `pwsh -NoProfile -Command "$s=Get-Content -Raw '.github/skills/technical-definition/SKILL.md'; $o=Get-Content -Raw '.github/agents/technical-orchestrator.agent.md'; $a=Get-Content -Raw '.github/agents/technical-author.agent.md'; $r=Get-Content -Raw '.github/agents/technical-reviewer.agent.md'; function Need($condition,$message){if(-not $condition){throw $message}}; Need ($o -match '(?m)^agents: \[technical-author, technical-reviewer\]$') 'orchestrator worker allowlist mismatch'; Need ($o -match '(?is)Delegate.*technical-author.*Delegate.*technical-reviewer') 'delegation sequence missing'; Need ($s -match '(?is)approved(?:-| )SOR') 'skill approved-SOR input missing'; Need ($s -match '(?is)Draft.*named non-author.*sign-off') 'skill Draft/non-author sign-off contract missing'; Need ($o -match '(?is)Report.*Draft') 'orchestrator Draft-status reporting missing'; Need ($o -match '(?is)(?:Do not|Never).*(?:sign off|sign-off)') 'orchestrator sign-off prohibition missing'; Need ($o -match '(?is)(?:Do not|Never).*approve') 'orchestrator approval prohibition missing'; Need ($a -match '(?is)technical-definition.*Draft') 'author-to-skill Draft contract missing'; Need ($a -match '(?is)(?:Do not|Never).*approve.*own') 'author self-approval prohibition missing'; Need ($r -match '(?is)read-only.*findings') 'reviewer read-only independence contract missing'; Need ($r -match '(?is)(?:Do not|Never).*approve') 'reviewer approval prohibition missing'; Need ($r -match '(?is)sign-off readiness') 'reviewer sign-off-readiness contract missing'; 'PASS: orchestration contract'"`; it exits `0` with the PASS message. | **Blocked** for an inconsistent delegation, role-specific Draft/sign-off/approval boundary, or planning boundary. Do not resolve by granting agents approval authority. | Ready for review |
| 8 | Run the structural Markdown check. | All five target files are readable, frontmatter-delimited, and contain exactly one H1. | From repository root, run `pwsh -NoProfile -Command "$files=@('.github/skills/technical-definition/SKILL.md','.github/agents/technical-orchestrator.agent.md','.github/agents/technical-author.agent.md','.github/agents/technical-reviewer.agent.md','.github/instructions/technical-and-planning-documents.instructions.md'); foreach($file in $files){$lines=Get-Content $file; if(-not ($lines.Count -ge 4 -and $lines[0] -eq '---' -and ($lines[1..($lines.Count-1)] -contains '---') -and ($lines | Where-Object {$_ -match '^# '}).Count -eq 1)){throw \"Invalid Markdown customization structure: $file\"}}; 'PASS: customization Markdown structure'"`; it exits `0` with `PASS: customization Markdown structure`. | **Blocked** when the structural check fails. Record the command result in Section 9's `Implementation Evidence Record`. | Ready for review |
| 9 | Observe VS Code instruction discovery for the representative matching and nonmatching paths. | When Customization Diagnostics, response References, or Agent Debug Logs are available, evidence shows `.github/instructions/technical-and-planning-documents.instructions.md` is discovered for `docs/technical/contracts/example.md` and is not discovered for `docs/planning/contracts/example.md`. | In VS Code, use any available Customization Diagnostics, response References, or Agent Debug Logs while targeting each named path. Record the positive and negative observations, or record `Unavailable: no Customization Diagnostics, response References, or Agent Debug Logs available` in Section 9's `Implementation Evidence Record`. | **Blocked** only when an available observation fails, or an actual scope mismatch is observed. When all diagnostics, references, and logs are unavailable, record the named limitation and leave this task **Ready for review**; unavailable evidence does not prove discovery. | Ready for review |
| 10 | Complete Section 9's `Implementation Evidence Record` for Martyn Fewtrell without approving or accepting the work item. | The named subsection identifies sources, five changed files, every contract-command result, VS Code evidence or limitation, scope-exclusion confirmation, the approved-SOR baseline reference, and Martyn Fewtrell's pending review, approval, and acceptance entries. | Inspect `### Implementation Evidence Record` in this exact plan and confirm every checklist field is completed or explicitly marked unavailable/pending as applicable. | **Blocked** for missing required evidence, a failed available check, actual scope mismatch, or a material decision not supplied; request the named human decision without approving or accepting the work item. | Ready for review |

## 7. Final Acceptance Check

- [ ] Exactly the five target files in the target map are created or changed; no excluded delivery-planning, GitHub Project/MCP, template, prompt, hook, Action, or plugin artifact is changed.
- [ ] The skill contract check verifies approved-SOR input, named destination/technical-owner/sign-off inputs, `prompt-me`, Draft-only direct `docs/technical/` output, technical-requirement and ADR minimum fields, verification/uncertainty/review content, human sign-off gate, SOR-change escalation, prohibited planning/approval actions, and permitted/invalid scenarios.
- [ ] Each agent contract check verifies filename/name consistency, exact tool allowlist, invocation mode, coordinator delegate allowlist, required procedure, forbidden actions, and a representative valid and stop scenario; the integration check confirms the preserved author/reviewer delegation, skill Draft plus named non-author sign-off, orchestrator Draft-status reporting with no sign-off/approval, author Draft-only/no self-approval, and reviewer read-only/no approval/sign-off readiness boundaries.
- [ ] The instruction contract check verifies frontmatter, the technical-only operational rules, and representative matching `docs/technical/contracts/example.md` and nonmatching `docs/planning/contracts/example.md` paths. It does not claim this path assertion proves VS Code instruction loading.
- [ ] Available VS Code Customization Diagnostics, response References, or Agent Debug Logs confirm or fail the positive observation for `docs/technical/contracts/example.md` and negative observation for `docs/planning/contracts/example.md`. If all are unavailable, `Implementation Evidence Record` names the limitation and Step 9 remains `Ready for review`; a failed available check or actual scope mismatch is `Blocked`.
- [ ] This exact plan's Section 9 `Implementation Evidence Record` links source authority, changed files, validation commands/results, exclusions, limitations, the approved-SOR baseline reference, and the Draft plan.
- [ ] Martyn Fewtrell records review, plan approval for implementation, and subsequent final work-item acceptance in this exact plan's Section 9 `Implementation Evidence Record`. The agent must not self-approve or self-accept.

## 8. Stop Conditions and Escalation

Stop and use the blocker report when:

- Martyn Fewtrell has not approved implementation of this Draft plan, or a source/target conflicts with its stated boundary.
- Martyn Fewtrell has not supplied the approved-SOR baseline reference required before edits.
- The proposed artifact would need a tool, delegate, file location, document template, technical category, sign-off authority, or validation claim not grounded in the source anchors or nearby repository pattern.
- The SOR supplied to the skill lacks explicit baseline approval, a named technical owner, a named sign-off authority, or an essential fact whose absence prevents a Draft baseline; route one question at a time through `prompt-me` or return the material change to the SOR workflow.
- A material business requirement changes during technical definition; do not edit the SOR or technical baseline as if approved. Return the change to the existing SOR workflow.
- A required contract assertion, structural check, or available VS Code diagnostics/references/logs check fails, or the instruction demonstrably has an actual scope mismatch.
- VS Code diagnostics/references/logs are unavailable: record the unavailable evidence and its limitation, keep the relevant task `Ready for review`, and obtain human review; do not mark it Blocked solely for that unavailable optional check.
- The work would add delivery-planning, GitHub Project/MCP, templates, prompts, hooks, Actions, plugins, external access, credentials, production action, or approval records.

Blocker report: `step`, `observed fact`, `command/output or path`, `impact`, `decision or input needed`, and `safe next action`.

## 9. File Handoff

- [ ] This plan is saved as a physical Markdown file at `docs/plans/technical-definition-artifacts.md`.
- [ ] The exact file path is reported to the orchestrator and human reviewer.
- [ ] The saved file is reopened or otherwise inspected after writing, and required sections 1 through 9 are present.

### Implementation Evidence Record

- [ ] Changed files: list exactly the five target-map paths, or state `None` while this Draft plan remains unimplemented.
- [ ] Commands and results: record each executed contract or structural command, its exit result, and its PASS or failure output.
- [ ] Unavailable checks: record each unavailable check and its named limitation, including `Unavailable: no Customization Diagnostics, response References, or Agent Debug Logs available` when applicable.
- [ ] Source links: list the source-document anchors recorded in Section 2.
- [ ] Scope-exclusion confirmation: confirm no excluded delivery-planning, GitHub Project/MCP, template, prompt, hook, Action, or plugin artifact changed.
- [ ] Approved-SOR baseline reference: record the supplied approved-SOR baseline reference required by Section 4; do not treat the example SOR as that supplied reference.
- [ ] Martyn Fewtrell review: `Pending | Recorded` with date and review evidence.
- [ ] Martyn Fewtrell implementation approval: `Pending | Recorded` with date and approval evidence.
- [ ] Martyn Fewtrell final work-item acceptance: `Pending | Recorded` with date and acceptance evidence.